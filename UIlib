local Library = {}
local UIS = game:GetService("UserInputService")
local TS = game:GetService("TweenService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

function Library:CreateWindow(Settings)
    local Window = {Tabs = {}}
    local Name = Settings.Name or "Whoami Lib"
    local Sub = Settings.LoadingSubtitle or "by user"
    
    local Screen = Instance.new("ScreenGui", LocalPlayer.PlayerGui)
    Screen.Name = "Whoami_Suite"
    Screen.IgnoreGuiInset = true

    -- // LOADING SCREEN //
    local LoadingFrame = Instance.new("CanvasGroup", Screen)
    LoadingFrame.Size = UDim2.new(0, 350, 0, 220)
    LoadingFrame.Position = UDim2.new(0.5, -175, 0.5, -110)
    LoadingFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
    LoadingFrame.GroupTransparency = 1
    Instance.new("UICorner", LoadingFrame).CornerRadius = UDim.new(0, 15)
    Instance.new("UIStroke", LoadingFrame).Color = Color3.fromRGB(40, 40, 40)

    local LTitle = Instance.new("TextLabel", LoadingFrame)
    LTitle.Text = Name; LTitle.Size = UDim2.new(1, 0, 0, 60); LTitle.Position = UDim2.new(0, 0, 0.15, 0)
    LTitle.TextColor3 = Color3.new(1, 1, 1); LTitle.Font = "GothamBold"; LTitle.TextSize = 28; LTitle.BackgroundTransparency = 1

    local LSub = Instance.new("TextLabel", LoadingFrame)
    LSub.Text = Sub; LSub.Position = UDim2.new(0, 0, 0.4, 0); LSub.Size = UDim2.new(1, 0, 0, 20)
    LSub.TextColor3 = Color3.fromRGB(180, 180, 180); LSub.Font = "Gotham"; LSub.TextSize = 14; LSub.BackgroundTransparency = 1

    local BarBack = Instance.new("Frame", LoadingFrame)
    BarBack.Size = UDim2.new(0.8, 0, 0, 4); BarBack.Position = UDim2.new(0.1, 0, 0.75, 0); BarBack.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    Instance.new("UICorner", BarBack)

    local BarFill = Instance.new("Frame", BarBack)
    BarFill.Size = UDim2.new(0, 0, 1, 0); BarFill.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    Instance.new("UICorner", BarFill)

    -- // MAIN UI (HIDDEN) //
    local Main = Instance.new("CanvasGroup", Screen)
    Main.Size = UDim2.new(0, 550, 0, 400)
    Main.Position = UDim2.new(0.5, -275, 0.5, -200)
    Main.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
    Main.Visible = false
    Main.GroupTransparency = 1
    Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 12)
    Instance.new("UIStroke", Main).Color = Color3.fromRGB(35, 35, 35)

    -- // Sidebar & Container //
    local SideFrame = Instance.new("Frame", Main)
    SideFrame.Size = UDim2.new(0, 150, 1, 0); SideFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    Instance.new("UICorner", SideFrame)

    local Sidebar = Instance.new("ScrollingFrame", SideFrame)
    Sidebar.Size = UDim2.new(1, -20, 1, -80); Sidebar.Position = UDim2.new(0, 10, 0, 70)
    Sidebar.BackgroundTransparency = 1; Sidebar.ScrollBarThickness = 0
    Instance.new("UIListLayout", Sidebar).Padding = UDim.new(0, 5)

    local Container = Instance.new("Frame", Main)
    Container.Size = UDim2.new(1, -170, 1, -20); Container.Position = UDim2.new(0, 160, 0, 10)
    Container.BackgroundTransparency = 1

    -- // TRANSITION LOGIC //
    task.spawn(function()
        TS:Create(LoadingFrame, TweenInfo.new(0.5), {GroupTransparency = 0}):Play()
        task.wait(0.5)
        local fill = TS:Create(BarFill, TweenInfo.new(2, Enum.EasingStyle.Quart), {Size = UDim2.new(1, 0, 1, 0)})
        fill:Play()
        fill.Completed:Wait()
        task.wait(0.3)
        TS:Create(LoadingFrame, TweenInfo.new(0.5), {GroupTransparency = 1}):Play()
        task.wait(0.5)
        LoadingFrame:Destroy()
        Main.Visible = true
        TS:Create(Main, TweenInfo.new(0.5), {GroupTransparency = 0}):Play()
    end)

    -- // Dragging //
    local dragging, dragInput, dragStart, startPos
    Main.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true; dragStart = input.Position; startPos = Main.Position
            input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end)
        end
    end)
    UIS.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - dragStart
            TS:Create(Main, TweenInfo.new(0.12, Enum.EasingStyle.Quad), {Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)}):Play()
        end
    end)

    -- // ONE LINE TAB CREATOR //
    function Window:CreateTab(TabName)
        local TabBtn = Instance.new("TextButton", Sidebar)
        TabBtn.Size = UDim2.new(1, 0, 0, 35); TabBtn.Text = "  "..TabName; TabBtn.BackgroundColor3 = Color3.fromRGB(20,20,20); TabBtn.TextColor3 = Color3.new(0.7,0.7,0.7); TabBtn.Font = "GothamMedium"; TabBtn.TextXAlignment = "Left"; TabBtn.AutoButtonColor = false
        Instance.new("UICorner", TabBtn); Instance.new("UIStroke", TabBtn).Color = Color3.fromRGB(35,35,35)

        local Page = Instance.new("ScrollingFrame", Container)
        Page.Size = UDim2.new(1, 0, 1, 0); Page.Visible = false; Page.BackgroundTransparency = 1; Page.ScrollBarThickness = 0
        Instance.new("UIListLayout", Page).Padding = UDim.new(0, 8)

        TabBtn.MouseButton1Click:Connect(function()
            for _, v in pairs(Container:GetChildren()) do if v:IsA("ScrollingFrame") then v.Visible = false end end
            Page.Visible = true
        end)
        if #Sidebar:GetChildren() == 2 then Page.Visible = true end

        local Elements = {Page = Page}
        function Elements:Button(txt, cb)
            local B = Instance.new("TextButton", self.Page)
            B.Size, B.Text = UDim2.new(1, -10, 0, 40), "  "..txt; B.BackgroundColor3, B.TextColor3 = Color3.fromRGB(22,22,22), Color3.new(0.9,0.9,0.9); B.Font, B.TextXAlignment = "GothamMedium", "Left"
            Instance.new("UICorner", B); Instance.new("UIStroke", B).Color = Color3.fromRGB(35,35,35)
            B.MouseButton1Click:Connect(cb)
            return self
        end
        function Elements:Toggle(txt, cb)
            local T = Instance.new("TextButton", self.Page)
            T.Size, T.Text = UDim2.new(1, -10, 0, 40), "  "..txt; T.BackgroundColor3, T.TextColor3 = Color3.fromRGB(22,22,22), Color3.new(0.9,0.9,0.9); T.Font, T.TextXAlignment = "GothamMedium", "Left"
            Instance.new("UICorner", T); Instance.new("UIStroke", T).Color = Color3.fromRGB(35,35,35)
            local s = false
            T.MouseButton1Click:Connect(function() s = not s; cb(s) end)
            return self
        end
        return Elements
    end
    return Window
end

return Library
